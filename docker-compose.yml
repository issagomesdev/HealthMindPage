version: '3.8'

services:
  healthmind-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-5173}:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    env_file:
      - .env.development
