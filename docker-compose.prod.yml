version: '3.8'

services:
  healthmind-web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "${APP_PORT:-80}:80"
    restart: unless-stopped
    env_file:
      - .env.production